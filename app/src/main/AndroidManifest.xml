<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android_ready">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:name="com.example.android_ready.MainApplication"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidReadyConceptDemo">
        <activity
            android:name="com.example.android_ready.DemoActivity"
            android:exported="true">
        </activity>

        <activity
            android:name="com.example.android_ready.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name="com.example.MyBroadCastReceiver.MyBroadCastReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.AIRPLANE_MODE" />-->
<!--            </intent-filter>-->
        </receiver>

    </application>

</manifest>